# Origin of the backend, without a trailing slash. Baked into the bundle at build time,
# so changing it requires a rebuild.
#
# Leave empty (or omit the file entirely) for local development — Vite proxies /api instead.
# In CI the value comes from the repository variable VITE_API_BASE.
VITE_API_BASE=https://java-quiz-backend.onrender.com
